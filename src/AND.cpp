#include "AND.h"

// class AND : public Connector {
//  public:

// Pushes back an "&" character into the connector vector.
void AND::setConVector(char* And) {
    connectors.push_back(And);
}

// Verifies that the && has been executed.
bool AND::execute() {

}